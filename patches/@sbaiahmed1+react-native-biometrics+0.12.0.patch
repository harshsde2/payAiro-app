diff --git a/node_modules/@sbaiahmed1/react-native-biometrics/android/build.gradle b/node_modules/@sbaiahmed1/react-native-biometrics/android/build.gradle
index 0a6a300..cd607c4 100644
--- a/node_modules/@sbaiahmed1/react-native-biometrics/android/build.gradle
+++ b/node_modules/@sbaiahmed1/react-native-biometrics/android/build.gradle
@@ -25,6 +25,13 @@ def getExtOrIntegerDefault(name) {
   return rootProject.ext.has(name) ? rootProject.ext.get(name) : (project.properties["ReactNativeBiometrics_" + name]).toInteger()
 }
 
+// Determine whether this library should run in New Architecture (TurboModules).
+// Apps can override via ReactNativeBiometrics_newArchEnabled in gradle.properties.
+def biometricsNewArchEnabled = project.findProperty("ReactNativeBiometrics_newArchEnabled")
+if (biometricsNewArchEnabled == null) {
+  biometricsNewArchEnabled = project.findProperty("newArchEnabled")
+}
+
 android {
   namespace "com.sbaiahmed1.reactnativebiometrics"
 
@@ -63,7 +70,7 @@ android {
     }
     
     // Support for both old and new architecture
-    if (project.findProperty("newArchEnabled") == "true") {
+    if (biometricsNewArchEnabled == "true") {
       main {
         java.srcDirs += ["src/newarch/java"]
       }
diff --git a/node_modules/@sbaiahmed1/react-native-biometrics/android/src/oldarch/java/com/sbaiahmed1/reactnativebiometrics/ReactNativeBiometricsModule.kt b/node_modules/@sbaiahmed1/react-native-biometrics/android/src/oldarch/java/com/sbaiahmed1/reactnativebiometrics/ReactNativeBiometricsModule.kt
index 3390218..4032af8 100644
--- a/node_modules/@sbaiahmed1/react-native-biometrics/android/src/oldarch/java/com/sbaiahmed1/reactnativebiometrics/ReactNativeBiometricsModule.kt
+++ b/node_modules/@sbaiahmed1/react-native-biometrics/android/src/oldarch/java/com/sbaiahmed1/reactnativebiometrics/ReactNativeBiometricsModule.kt
@@ -21,11 +21,6 @@ class ReactNativeBiometricsModule(reactContext: ReactApplicationContext) :
 
   override fun getName(): String = "ReactNativeBiometrics"
 
-  @ReactMethod
-  fun isSensorAvailable(promise: Promise) {
-    sharedImpl.isSensorAvailable(promise)
-  }
-
   @ReactMethod
   fun isSensorAvailable(biometricStrength: String?, promise: Promise) {
     sharedImpl.isSensorAvailable(biometricStrength, promise)
